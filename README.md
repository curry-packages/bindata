bindata: Library to support reading/writing binary data
=======================================================

This package contains the library `BinaryFile` which provides
some operations to read and write files containing binary data.

